{
  "installCommand": "npm install",
  "buildCommand": "node -e \"console.log('platform:', process.platform, 'arch:', process.arch); const p=require('esbuild'); console.log('esbuild version:', p.version)\" && npx --no-install esbuild --version && npm run build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
